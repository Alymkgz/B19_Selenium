package selenium.locators;

public class CSSLocators {
    /*
      Xpath syntax --> //tagName[@attribute='value']

      CSS locator syntax --> tagName[attribute='value']


        #id --> ID
        .className --> Class

        Parent/Child relation in CSS -->  > < <
        Sibling relationship --> ~ +
     */
}
